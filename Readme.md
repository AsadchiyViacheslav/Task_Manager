# Task Manager 🗃️

Необходимо реализовать backend часть проекта на Java с использованием Spring Boot и PostgreSQL. Архитектура должна быть чистой и понятной (с разделением на .env, dto, repository, model, service, controller и так далее).

Необходимо реализовать регистрацию, авторизацию и выход из аккаунта.

Сущность пользователя содержит: id, username, email, passwordHash, createdAt.

При регистрации нужно проверять, что два введённых пароля совпадают, проводить валидацию, что пользователя с таким email нет, что email имеет корректную форму, длина email максимум 254 символа, пароль максимум 50 символов, минимум 8 символов, среди которых есть заглавные и строчные буквы.

Используем два токена: access и refresh, оба хранятся в HttpOnly и Secure cookies, отдельный endpoint для работы с токенами - refresh, стандартная логика: при истечении access токена фронт делает запрос на refresh endpoint, backend проверяет refresh токен и выдаёт новую пару токенов.

Выход из аккаунта реализуется удалением refresh токена из базы.

Также необходимо реализовать работу с задачами.

Задачи и подзадачи:

Задача содержит: id, title, description, deadline (дата без времени), priority (лучше enum), createdAt, creatorId, status (лучше enum). У задачи может быть одно фото, которое задается при создании или редактировании, отдельная таблица для фото с taskId. Фотографии хранятся локально на сервере, необходимо настроить статическую раздачу папки, получение задач возвращает также путь к фото для фронтенда. При загрузке фото должна быть проверка наличия файла. Название задачи максимум 100 символов, описание задачи максимум 1000 символов.

Подзадача содержит: id, parentId, description, status (выполнено/не выполнено), можно ограничить длину description при необходимости.

Для API и backend части предусмотреть:

* Endpoints для регистрации, логина, выхода, обновления токенов.
* Endpoints для создания, редактирования, удаления, получения всех задач пользователя, получения конкретной задачи.
* Endpoints для добавления, изменения, удаления фото задачи.
* Endpoints для работы с подзадачами: создание, редактирование, удаление, получение.
* Валидацию входных данных на backend.
* Корректную работу с токенами в cookies.
* Безопасность: пароль хранить в хэшированном виде (BCrypt или аналог), защита от прямого доступа к фото через правильную настройку статической раздачи.
* Spring Boot Exception handler чтобы не писать постоянно try except и удобно возвращать ошибки

Дополнительно:

* При удалении задачи удалять связанное фото и подзадачи.
* Логику CRUD реализовать через сервисный слой.
* Структура проекта должна быть готова к расширению, чистая архитектура, понятные DTO, маппинг между сущностями и DTO.
* Предусмотреть обработку ошибок и возвращение понятных сообщений клиенту (на фронтенд возвращать сразу готовый текст ошибки error: text).
* Для полей status и priority использовать enum для лучшей типизации.
* Сделать Docker 
